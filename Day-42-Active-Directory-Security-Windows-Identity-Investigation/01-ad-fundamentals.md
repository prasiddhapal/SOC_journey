# 01 | Active Directory Fundamentals

A domain provides centralized identity and resource-management boundaries.

A Domain Controller runs AD DS and participates in authenticating domain identities. In a Kerberos environment it also provides the KDC.

A user account represents a domain identity. A computer account represents a domain-joined machine.

SOC relevance: user, computer, domain and DC relationships provide the context needed to interpret authentication alerts.
