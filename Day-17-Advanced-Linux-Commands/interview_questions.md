# Interview Questions – Day 17

## Technical Questions

### journalctl

- What is journalctl?
- Difference between `journalctl -b` and `journalctl -u`?

---

### awk

- Why use awk instead of grep?
- Explain `$1`, `$2`, and `NR`.

---

### sed

- Difference between `sed` and `less`.
- What does `-i` do?
- Why avoid `-i` during investigations?

---

### tr

- Difference between `tr` and `sed`.
- What does `tr -d` do?
- What does `tr -s` do?

---

### tee

- Difference between `>` and `tee`.
- Purpose of `tee -a`.
- Why use `tee` during incident response?

---

### xargs

- What does xargs do?
- Why combine `find` and `xargs`?
- Why use `find -print0 | xargs -0`?

---

## HR Question

Why is understanding Linux commands more valuable than memorizing them?

### Suggested Answer

Understanding allows analysts to troubleshoot problems, adapt commands to new scenarios, avoid operational mistakes, and explain their actions during incident response.

---

## Final Scores

| Module | Score |
|---------|------:|
| journalctl | 48/100 |
| awk | 94/100 |
| sed | 95/100 |
| tr | 91/100 |
| tee | 100/100 |
| xargs | 95/100 |

## Overall Day Score

**94/100 (A)**
